  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # ... các bước build của bạn ...
      - name: Upload artifact
        uses: actions/actions-deploy-pages/v4 # Hoặc actions/upload-pages-artifact@v3
        with:
          path: '.' # Đường dẫn thư mục chứa code đã build
